workflowのFAILサンプル誤検知（delete_everywhere）を修正し、`aa095ec` を master に反映しました。
`sync-log-keys-check` は `scenario=both` / `scenario=fail` ともに success（Run: 22513165210, 22513547225）、さらに最新 `ceb72db` でも `both` success（Run: 22513723835）を確認済みです。
監査提出用CSV `docs/audit_submission_form.csv` を追加済み（`ceb72db`）、作業ツリーは clean です。
